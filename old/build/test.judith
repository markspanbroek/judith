object bar := Object |[

    object string := '1'

]|

object foo := bar |[

    method init [
        # todo: i'd prefer that this didn't work:
        string := '2'
    ]

]|.init

Object.assert(foo:bar)
Object.assert(foo:bar:Boolean)
